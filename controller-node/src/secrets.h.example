/*
 * Secrets Configuration Template - Controller Node
 *
 * Copy this file to secrets.h and fill in your actual values.
 * DO NOT commit secrets.h to version control!
 */

#ifndef SECRETS_H
#define SECRETS_H

// OTA Update Password
// Used for over-the-air firmware updates
#define OTA_PASSWORD "your_ota_password_here"

// MQTT Configuration
#define MQTT_SERVER "192.168.1.100"       // Your MQTT broker IP
#define MQTT_USER "your_mqtt_username"
#define MQTT_PASS "your_mqtt_password"

// Static IP Configuration (controller node only)
// These define the network settings when USE_STATIC_IP is enabled in main.cpp
// Format: comma-separated octets (e.g., 192, 168, 1, 50)
#define STATIC_IP_ADDR     192, 168, 1, 50    // Controller's static IP
#define STATIC_IP_GATEWAY  192, 168, 1, 1     // Router IP
#define STATIC_IP_SUBNET   255, 255, 255, 0
#define STATIC_IP_DNS      192, 168, 1, 1     // Usually same as gateway

#endif // SECRETS_H
